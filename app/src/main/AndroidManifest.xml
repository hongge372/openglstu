<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="triangle.opengl.wlz.stu.myapplication_1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".costomview.CostomView"></activity>
        <activity android:name=".gles.BackgroundRed"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".gles.DrawPoint" />
        <activity android:name=".gles.LineSegment"/>
        <activity android:name=".gles.Trangles" />
        <activity android:name=".gles.Pholyhedron" />
    </application>

</manifest>